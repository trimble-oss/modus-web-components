export enum ModusColumnDataType {
  Text = 'text',
  Integer = 'integer',
  // To support in the future
  // Currency = 'currency',
  // Percent = 'percent',
  // Decimal = 'decimal',
  // Date = 'date',
  // Dropdown = 'dropdown',
}
