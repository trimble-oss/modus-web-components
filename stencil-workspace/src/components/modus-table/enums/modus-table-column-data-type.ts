export enum ModusTableColumnDataType {
  Text = 'text',
  Integer = 'integer',
  Boolean = 'boolean',
  Link = 'link',
  Badge = 'badge',
  // To support in the future
  // Currency = 'currency',
  // Percent = 'percent',
  // Decimal = 'decimal',
  // Date = 'date',
  // Dropdown = 'dropdown',
}
