export enum ModusTableColumnDataType {
  Text = 'text',
  Integer = 'integer',
  Link = 'link',
  Date = 'date',
  // To support in the future
  // Currency = 'currency',
  // Percent = 'percent',
  // Decimal = 'decimal',
  // Dropdown = 'dropdown',
}
